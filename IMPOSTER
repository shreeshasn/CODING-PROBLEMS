#include <stdio.h>
int main() {
    int a[]={55,5,5,5,5,5,5,5,5}, key=0;
    if(a[0]==a[1])    
        key=a[0];
    for(int i=0; i<(sizeof(a)/sizeof(a[0])); i++)
    {
        if(key!=a[i])
        {
            printf("%d", a[i]);
            break;
        }
    }
    return 0;
}
